<html>
    <head>
        <link rel="stylesheet" href="/css/index.css" >
    </head>
    <body>
        <div class="logo">
            <img src="/img/ex.jpeg" />
        </div>
        <div class="nama">
            <%= nama  %>
            <ul class="skill">
            <% skill.forEach(element => { %>
                <li><%= element.skillName %>:<%= element.level %></li>
            <% }); %>
            </ul>
        </div>
    </body>
</html>